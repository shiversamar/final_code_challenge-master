require 'rails_helper'

RSpec.describe CommentsController, type: :controller do
  describe "Post create" do

      # it "create a new post" do
      #   expect {post(:create,(comment:comment_params))}.to change
      # end
      #
      # it "create a post and comment with user_id " do
      #   post(:create, {comment:comment_params})
      #
      #   @comment = Comment.last
      #   expect(@coment(:body)).to match_array([comment_params,body])
      #   expect(@comment.author_name).to match_array([:user.email])
      # end

    end#describe
  end#Rspec
